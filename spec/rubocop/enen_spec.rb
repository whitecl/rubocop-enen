# frozen_string_literal: true

RSpec.describe Rubocop::Enen do
  it "has a version number" do
    expect(Rubocop::Enen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
